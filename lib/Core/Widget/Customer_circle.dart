import 'package:flutter/material.dart';

class CustomerCircle extends StatelessWidget {
  const CustomerCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
